{
  "name": "lumera-store",
  "version": "1.0.0",
  "private": true,
  "description": "LUMÉRA — Luxury Fashion E-commerce (Frontend + Admin + Serverless API)",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step required'",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "xlsx": "^0.18.5",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}